# LinkedIn credentials (optional — enables authenticated scraping for more results)
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

# Proxy (optional — helps avoid rate-limiting on large searches)
# PROXY=http://user:pass@host:port

# Database location (defaults to data/jobs.db)
# DB_PATH=data/jobs.db
